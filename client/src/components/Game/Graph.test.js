import { Graph } from './Graph';

describe('Graph', () => {
  test('should add 1 + 2 to equal 3', () => {
    expect(Graph(1, 2)).toBe(3);
  });
});

describe('Graph', () => {
  test('should add 1 + 2 to equal 3', () => {
    expect(Graph(1, 2)).toBe(6);
  });
});
